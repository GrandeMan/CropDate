import { Link } from "react-router-dom";

// const MyCrops = ({ favoriteCrops }) => {
const MyCrops = () => {
  return (
    // <div>
    //   <h1>My Crops</h1>
    //   {favoriteCrops.length === 0 ? (
    //     // Render when the favorites list is empty
    //     <div>
    //       <p>Your favorites list is empty. Start adding crops you like!</p>
    //       <Link to="/all-crops">
    //         <button>Explore All Crops</button>
    //       </Link>
    //     </div>
    //   ) : (
    //     // Render when there are items in the favorites list
    //     <div>
    //       <p>Your favorite crops:</p>
    //       <ul>
    //         {favoriteCrops.map((crop, index) => (
    //           <li key={index}>{crop.name}</li>
    //         ))}
    //       </ul>
    //       {/* Add more content or components for displaying favorite crops */}
    //     </div>
    //   )}
    // </div>
    <div>Hello</div>
  );
};

export default MyCrops;
